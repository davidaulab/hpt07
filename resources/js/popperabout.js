import { createPopper } from '@popperjs/core';

/*
const whatsapp = document.querySelector('#whatsapp');
const tooltip = document.querySelector('#tooltip');

createPopper(whatsapp, tooltip, {
  placement: 'right',
  modifiers: [
    {
      name: 'offset',
      options: {
        offset: [0, 8],
      },
    },
  ],
});
*/